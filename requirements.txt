python-telegram-bot==20.6          # برای ربات تلگرام
gTTS==2.3.2                        # تبدیل متن به صوت
playsound==1.3.0                    # پخش صوت در لوکال (اختیاری برای تست)
requests==2.31.0                    # برای درخواست‌های HTTP به وبسایت یا API
python-dotenv==1.0.1                # مدیریت متغیرهای محیطی (توکن و تنظیمات)
